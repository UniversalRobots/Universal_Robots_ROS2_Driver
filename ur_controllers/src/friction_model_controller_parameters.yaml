---
friction_model_controller:
  tf_prefix: {
    type: string,
    default_value: "",
    description: "Urdf prefix of the corresponding arm"
  }
  check_io_successful_retries: {
    type: int,
    default_value: 10,
    description: "Amount of retries for checking if setting friction scales was successful"
  }
